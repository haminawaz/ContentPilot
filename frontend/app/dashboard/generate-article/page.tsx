import { GenerateArticleClient } from "@/components/dashboard/article/GenerateArticleClient";

export const metadata = {
  title: "Generate Article",
};

export default function GenerateArticlePage() {
  return <GenerateArticleClient />;
}
